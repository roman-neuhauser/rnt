successful test case run

if all .actual files match expectations, run-test.sh should
not output anything on either stdout or stderr and its exit
code should indicate success.
it should wipe out *.actual, *.diff and *.diff.tmp files.
